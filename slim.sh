#!/usr/bin/env bash

sudo apt install -y slim
git clone https://github.com/adi1090x/slim_themes.git

# 1. copy themes to /usr/share/slim/themes/
# 2. change /etc/slim.conf settings
# 3. append /sbin to .profile PATH
